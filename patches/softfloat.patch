diff --git a/lib/libc/arch/aarch64/softfloat/qp.c b/lib/libc/arch/aarch64/softfloat/qp.c
index aa5a7fe39..fdf1d1cf1 100644
--- a/lib/libc/arch/aarch64/softfloat/qp.c
+++ b/lib/libc/arch/aarch64/softfloat/qp.c
@@ -87,6 +87,7 @@ union sf_ieee_ldbl_u {
 	float128 ldblu_f128;
 };
 
+#if 0
 long double
 __addtf3(long double ld_a, long double ld_b)
 {
@@ -98,6 +99,7 @@ __addtf3(long double ld_a, long double ld_b)
 
 	return c.ldblu_ld;
 }
+#endif
 
 long double
 __divtf3(long double ld_a, long double ld_b)
@@ -240,6 +242,7 @@ __extendsftf2(float f_a)
 	return c.ldblu_ld;
 }
 
+#if 0
 long double
 __extenddftf2(double d_a)
 {
@@ -250,6 +253,7 @@ __extenddftf2(double d_a)
 
 	return c.ldblu_ld;
 }
+#endif
 
 long double
 __floatunsitf(uint32_t a)
@@ -290,6 +294,7 @@ __floatsitf(int32_t a)
 	return c.ldblu_ld;
 }
 
+#if 0
 long double
 __floatditf(int64_t a)
 {
@@ -299,3 +304,4 @@ __floatditf(int64_t a)
 
 	return c.ldblu_ld;
 }
+#endif
diff --git a/sys/kern/uipc_sem.c b/sys/kern/uipc_sem.c
index 094979517..482281197 100644
--- a/sys/kern/uipc_sem.c
+++ b/sys/kern/uipc_sem.c
@@ -87,8 +87,8 @@ MODULE(MODULE_CLASS_MISC, ksem, NULL);
 
 #define	SEM_MAX_NAMELEN		NAME_MAX
 
-# https://muc.lists.netbsd.bugs.narkive.com/2z13sHNT/kern-53998-sem-init-fails-with-error-1
-# allow increase of SEM_NSEMS_MAX (incorrect error reported, see above)
+// https://muc.lists.netbsd.bugs.narkive.com/2z13sHNT/kern-53998-sem-init-fails-with-error-1
+// allow increase of SEM_NSEMS_MAX (incorrect error reported, see above)
 #ifndef SEM_NSEMS_MAX
 #define	SEM_NSEMS_MAX		256
 #endif
